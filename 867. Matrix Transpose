class Solution:
    def transpose(self, matrix: List[List[int]]) -> List[List[int]]:
        m=len(matrix)
        n=len(matrix[0])
        row=0
        mat = [[0] * m for _ in range(n)]
        print(mat)
        while (row<m):
            for i in range(0,n):
                mat[i][row]=matrix[row][i]
            row=row+1
        return mat
        
